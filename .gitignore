# Resolved per machine, not shared: this is a library, not an application.
Manifest.toml

# Coverage and allocation artefacts.
*.jl.cov
*.jl.*.cov
*.jl.mem
lcov.info

# The gate's per-run scratch output. The baselines and rulings.toml beside them
# are committed; these are not.
code_ratchet/_refresh/
code_ratchet/_triage/
